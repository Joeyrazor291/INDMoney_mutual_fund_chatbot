[build]
builder = "nixpacks"
buildCommand = "playwright install chromium --with-deps && python phase_1/chunker.py"

[deploy]
startCommand = "uvicorn phase_4.backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
