[build]
builder = "nixpacks"
buildCommand = "python3 phase_1/chunker.py"

[deploy]
startCommand = "python3 -m playwright install chromium --with-deps && uvicorn phase_4.backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
